<UserControl x:Class="OpcClient.Views.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5" Grid.Row="0">
            <Button Content="Refresh"
                    Command="{Binding DataContext.TagsVM.RefreshCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    Width="60"
                    Height="20"
                    Margin="0,0,5,0"
                    HorizontalAlignment="Left"/>
            <CheckBox Content="Auto Refresh"
                      IsChecked="{Binding AutoRefresh}"
                      Margin="0,0,5,0"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center"/>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
            <Label Content="DisplayName filter:"
                   VerticalAlignment="Center"
                   Height="17"
                   HorizontalAlignment="Right"
                   Padding="0,0,0,0"
                   Margin="5,0,0,0"/>
            <TextBox Width="200"
                     Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Filter by DisplayName"
                     Margin="5,0,5,0"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Tags}"
                      Margin="5"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HorizontalGridLinesBrush="DarkGray"
                      RowBackground="LightGray"
                      AlternatingRowBackground="White"
                      Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="DisplayName" Binding="{Binding DisplayName}"/>
                <DataGridTextColumn Header="NodeId" Binding="{Binding NodeId}"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}"/>
                <DataGridTextColumn Header="DataType" Binding="{Binding DataType}"/>
                <DataGridTextColumn Header="StatusCode" Binding="{Binding StatusCode}">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="{Binding StatusCode, Converter={StaticResource QualBrush}}"/>
                            <Setter Property="Padding" Value="4"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="SourceTimestamp" Binding="{Binding SourceTimestamp}"/>
                <DataGridTextColumn Header="ServerTimestamp" Binding="{Binding ServerTimestamp}"/>
                <DataGridTextColumn Header="Age" Binding="{Binding Age}"/>
                <DataGridTextColumn Header="Note" Binding="{Binding Note}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar DockPanel.Dock="Bottom" Grid.Row="2">
            <StatusBarItem Content="{Binding Error}" Foreground="Red"/>
        </StatusBar>
    </Grid>
</UserControl>
